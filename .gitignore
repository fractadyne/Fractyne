bin/
*.o

# generated by `fractyne build examples/*.fy` -- keep only the source and
# expected-output files; ignore anything else that shows up in examples/
# (the compiled binary has no extension, so a blacklist of names goes stale
# every time a new example is added -- this whitelist can't)
examples/*
!examples/*.fy
!examples/*.expected
